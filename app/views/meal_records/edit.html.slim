- content_for(:title, t('.title'))
h1
  = t('.title')

.ui.container
  .ui.segment
    .ui.centered.grid
      .twelve.wide.column
        .ui.text.container
          = @meal_record.food.name
      .twelve.wide.column
        .ui.text.container
          = "#{@meal_record.food.calorie} kcal"
      .twelve.wide.column
        .ui.text.container
          = @meal_record.food.calorie_theory
      .twelve.wide.column
        .ui.text.container
          .ui.form
            = form_with model: @meal_record, class: 'ui form', local: true do |f|
              .label
                = f.label :meal_time
              .field
                = f.datetime_field :meal_time
              .label
                = f.label :meal_record_picture
              .field
                = f.file_field :meal_record_picture
                - if @meal_record.meal_record_picture.attached?
                  = image_tag @meal_record.meal_record_picture
              .actions
                = f.button t('defaults.update'), class: 'ui button'
              .actions
                = link_to t('defaults.back'), meal_records_path, class: 'ui button'
